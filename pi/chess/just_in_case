int r_plays[reps*4*SOI], rp_i=0,
            r_values[reps*SOI];
		for (int d=0;d<_plays_no;d++) 
			if (diffs[d]==diffs[i_winner]) {
				r_plays[rp_i]=_plays[d*4],
				r_plays[rp_i+1]=_plays[(d*4)+1],
				r_plays[rp_i+2]=_plays[(d*4)+2],
				r_plays[rp_i+3]=_plays[(d*4)+3];
				rp_i+=4;
			}
		get_values(r_plays,r_values,(int)(rp_i*.25F),table,true);
		int p=3,
			r_diffs[(int)(rp_i*.25F)*SOI], rd_i=0,
			o_turn=n_turn;
		printf("[%d]\n",(int)(rp_i*.25F));
		while (p<rp_i) {
			int r_sx=r_plays[p-3], r_sy=r_plays[p-2],
			    r_dx=r_plays[p-1], r_dy=r_plays[p],
			    tmp_table[TS*SOI];
			copy_table(tmp_table,table);
			chg_pos(tmp_table,r_sx,r_sy,r_dx,r_dy);
			(void)swap_player(false);
			int i_w=MRC-r_values[(int)(p*.25F)],
			    i_l=MRC-win_or_lose(tmp_table,o_turn),
			    rat_i_w=MRC-win_or_lose(tmp_table,o_turn), 
			    rat_i_l=MRC-win_or_lose(tmp_table,o_turn);
			r_diffs[rd_i++]=i_w-i_l+rat_i_w-rat_i_l;
			//printf("\n\033[0;35m(%d, %d -> %d, %d)",
			//   r_sx,r_sy,r_dx,r_dy);
			printf("\n\033[0;35m%d\033[0m",
			    //i_w,i_l,rat_i_w,rat_i_l,
			    i_w-i_l+rat_i_w-rat_i_l);getch();
			p+=4;
		}

